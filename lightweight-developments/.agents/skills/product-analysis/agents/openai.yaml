interface:
  display_name: "Product Analysis (NIU)"
  short_description: "Analyze production data with read-only safeguards"
  brand_color: "#2563EB"
  default_prompt: "Use $product-analysis to answer this production-data question through bounded, enforced read-only investigation."
